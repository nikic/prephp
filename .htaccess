RewriteEngine on
Options +FollowSymLinks

#disallow access to cache and to prephp files
RewriteRule ^cache/ - [F]

# prephp gets the variables
# prefixed with prephp_
# path: relative to this .htaccess
RewriteRule ^(.*\.php5?)$ prephp/prephp.php?prephp_path=$1 [QSA,L]